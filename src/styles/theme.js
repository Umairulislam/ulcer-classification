import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    primary: {
      main: "#00A9FF",
      light: "#80D4FF",
    },
    secondary: {
      main: "#D3D3D3",
      light: "#EBEBEB",
    },
  },
})

export default theme
